{
  "name": "finnice-app-backend-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix src run dev",
    "start": "npm --prefix src start",
    "setup-db": "npm --prefix src run setup-db"
  }
}
